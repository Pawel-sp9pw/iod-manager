<x-layouts.app><a href="{{ route('companies.show',$company) }}" class="text-sm text-slate-400">← {{ $company->name }}</a><h1 class="text-3xl font-semibold mt-3 mb-6">Rejestry RODO</h1><div class="grid gap-3 md:grid-cols-2 mb-8">@foreach($registers as $r)<a href="{{ route('registers.show',[$company,$r]) }}" class="rounded-xl border border-slate-800 bg-slate-900 p-4"><div class="font-semibold">{{ $r->name }}</div><div class="text-sm text-slate-500">{{ $r->type }} · {{ $r->entries_count }} wpisów</div></a>@endforeach</div>@if(auth()->user()->canWriteCompany($company->id))<form method="POST" action="{{ route('registers.store',$company) }}" class="rounded-xl border border-slate-800 p-5 grid gap-3 md:grid-cols-2">@csrf<h2 class="text-xl font-semibold md:col-span-2">Nowy rejestr</h2><input name="name" required placeholder="Nazwa rejestru" class="rounded bg-slate-900 border border-slate-700 px-3 py-2"><select name="type" class="rounded bg-slate-900 border border-slate-700 px-3 py-2"><option value="processing_activities">Czynności przetwarzania</option><option value="breaches">Naruszenia</option><option value="data_subject_requests">Żądania osób</option><option value="processors">Podmioty przetwarzające</option><option value="dpia_risk">DPIA / ryzyko</option><option value="training">Szkolenia</option><option value="inspections">Kontrole</option><option value="other">Inny</option></select><textarea name="description" placeholder="Opis" class="rounded bg-slate-900 border border-slate-700 px-3 py-2 md:col-span-2"></textarea><button class="rounded bg-slate-100 text-slate-950 px-4 py-2 font-medium md:col-span-2">Utwórz rejestr</button></form>@endif</x-layouts.app>
