<x-layouts.app><a href="{{ route('dashboard') }}" class="text-sm text-slate-400 hover:text-white">← Wszystkie firmy</a><div class="mt-4 mb-8"><h1 class="text-3xl font-semibold">{{ $company->name }}</h1><p class="text-slate-400">{{ $company->city }} @if($company->nip) · NIP {{ $company->nip }} @endif</p></div><div class="grid gap-4 md:grid-cols-3 mb-8"><div class="rounded-xl border border-slate-800 bg-slate-900 p-5"><div class="text-sm text-slate-400">Rejestry</div><div class="text-3xl mt-2">{{ $registersCount }}</div></div><div class="rounded-xl border border-slate-800 bg-slate-900 p-5"><div class="text-sm text-slate-400">Aktywne upoważnienia</div><div class="text-3xl mt-2">{{ $activeAuthorizationsCount }}</div></div><div class="rounded-xl border border-slate-800 bg-slate-900 p-5"><div class="text-sm text-slate-400">Najbliższe przypomnienia</div><div class="text-3xl mt-2">{{ $upcomingReminders->count() }}</div></div></div><section><h2 class="text-xl font-semibold mb-4">Najbliższe terminy</h2><div class="rounded-xl border border-slate-800 divide-y divide-slate-800">@forelse($upcomingReminders as $reminder)<div class="p-4 flex justify-between gap-4"><span>{{ $reminder->title }}</span><span class="text-slate-400 text-sm">{{ optional($reminder->next_due_at ?: $reminder->due_at)->format('d.m.Y H:i') }}</span></div>@empty<div class="p-4 text-slate-400">Brak aktywnych przypomnień.</div>@endforelse</div></section></x-layouts.app>
