<x-layouts.app><div class="flex justify-between gap-4"><div><a href="{{ route('dashboard') }}" class="text-sm text-slate-400">← Wszystkie firmy</a><h1 class="text-3xl font-semibold mt-3">{{ $company->name }}</h1><p class="text-slate-400">{{ $company->city }} @if($company->nip) · NIP {{ $company->nip }} @endif</p></div>@if(auth()->user()->is_super_admin)<a href="{{ route('companies.edit',$company) }}" class="h-fit rounded-lg border border-slate-700 px-4 py-2">Edytuj firmę</a>@endif</div><div class="grid gap-4 md:grid-cols-3 my-8"><a href="{{ route('registers.index',$company) }}" class="rounded-xl border border-slate-800 bg-slate-900 p-5"><div class="text-sm text-slate-400">Rejestry</div><div class="text-3xl mt-2">{{ $registersCount }}</div></a><a href="{{ route('authorizations.index',$company) }}" class="rounded-xl border border-slate-800 bg-slate-900 p-5"><div class="text-sm text-slate-400">Aktywne upoważnienia</div><div class="text-3xl mt-2">{{ $activeAuthorizationsCount }}</div></a><a href="{{ route('reminders.index',$company) }}" class="rounded-xl border border-slate-800 bg-slate-900 p-5"><div class="text-sm text-slate-400">Najbliższe przypomnienia</div><div class="text-3xl mt-2">{{ $upcomingReminders->count() }}</div></a></div><section class="mb-8"><h2 class="text-xl font-semibold mb-4">Najbliższe terminy</h2><div class="rounded-xl border border-slate-800 divide-y divide-slate-800">@forelse($upcomingReminders as $r)<div class="p-4 flex justify-between"><span>{{ $r->title }}</span><span class="text-sm text-slate-400">{{ ($r->next_due_at ?: $r->due_at)?->format('d.m.Y H:i') }}</span></div>@empty<div class="p-4 text-slate-400">Brak aktywnych przypomnień.</div>@endforelse</div></section>@if(auth()->user()->is_super_admin)<section class="rounded-xl border border-slate-800 p-5"><h2 class="text-xl font-semibold mb-4">Konta przypisane do firmy</h2><div class="space-y-2 mb-6">@foreach($companyUsers as $u)<div class="flex justify-between items-center border-b border-slate-800 pb-2"><div>{{ $u->name }} <span class="text-slate-500">{{ $u->email }} · {{ $u->pivot->role }}{{ $u->pivot->can_write?' · zapis':'' }}</span></div>@if($u->id!==auth()->id())<form method="POST" action="{{ route('companies.users.destroy',[$company,$u]) }}">@csrf @method('DELETE')<button class="text-sm text-red-300">Usuń dostęp</button></form>@endif</div>@endforeach</div><form method="POST" action="{{ route('companies.users.store',$company) }}" class="grid gap-3 md:grid-cols-2">@csrf<input name="name" required placeholder="Imię i nazwisko" class="rounded bg-slate-900 border border-slate-700 px-3 py-2"><input type="email" name="email" required placeholder="E-mail" class="rounded bg-slate-900 border border-slate-700 px-3 py-2"><input type="password" name="password" required placeholder="Hasło min. 14 znaków" class="rounded bg-slate-900 border border-slate-700 px-3 py-2"><input type="password" name="password_confirmation" required placeholder="Powtórz hasło" class="rounded bg-slate-900 border border-slate-700 px-3 py-2"><select name="role" class="rounded bg-slate-900 border border-slate-700 px-3 py-2"><option value="company_admin">Administrator — podgląd</option><option value="iod">IOD — pełny dostęp</option></select><label class="flex items-center gap-2"><input type="checkbox" name="can_write" value="1"> Zezwól na zapis</label><button class="rounded bg-slate-100 text-slate-950 px-4 py-2 font-medium md:col-span-2">Dodaj / przypisz konto</button></form></section>@endif</x-layouts.app>
