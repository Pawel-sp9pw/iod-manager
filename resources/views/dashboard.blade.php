<x-layouts.app><div class="flex items-end justify-between mb-8"><div><p class="text-sm uppercase tracking-widest text-slate-500">Panel IOD</p><h1 class="text-3xl font-semibold">Firmy</h1></div><div class="text-sm text-slate-400">{{ $companies->count() }} aktywnych</div></div><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($companies as $company)<a href="{{ route('companies.show', $company) }}" class="rounded-xl border border-slate-800 bg-slate-900 p-5 hover:border-slate-600 transition"><div class="font-semibold text-lg">{{ $company->short_name ?: $company->name }}</div><div class="mt-1 text-sm text-slate-400">{{ $company->name }}</div><div class="mt-4 text-xs text-slate-500">{{ $company->city }} @if($company->nip) · NIP {{ $company->nip }} @endif</div></a>@empty<div class="text-slate-400">Brak przypisanych firm.</div>@endforelse</div></x-layouts.app>
